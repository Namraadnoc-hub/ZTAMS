<x-layouts.app><h1>School Attendance Pulse</h1><p>{{ $classes->count() }} classes · {{ $classes->sum('students_count') }} students · {{ $todaySessions->where('status','submitted')->count() }} submitted today</p><table><tr><th>Class</th><th>Teacher</th><th>Students</th><th>Status</th></tr>@foreach($classes as $class)<tr><td><a href="{{route('administrator.class',$class)}}">{{ $class->name }}</a></td><td>{{ $class->currentAssignment?->teacher?->full_name ?? 'Unassigned' }}</td><td>{{ $class->students_count }}</td><td>{{ optional($todaySessions->firstWhere('class_id',$class->id))->status ?? 'Pending' }}</td></tr>@endforeach</table></x-layouts.app>
