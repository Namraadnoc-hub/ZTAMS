<x-layouts.app><h1>Assalam-o-Alaikum, {{ $teacher->full_name }} 👋</h1><p class="muted">{{ now(config('app.timezone'))->format('l, d F Y') }}</p><div class="card"><h2>{{ $class->name }}</h2><p>30 students · Your assigned class</p><p><span class="badge">{{ $today?->status ?? 'Not started' }}</span> @if($today?->isLocked()) Attendance locked at 9:00 AM. @else Attendance can be edited until 9:00 AM. @endif</p><a class="btn" href="{{route('teacher.attendance.show',$class)}}">Take Attendance</a></div><div class="grid"><div class="card"><h3>My Students</h3><a class="btn alt" href="{{route('teacher.students',$class)}}">View performance</a></div><div class="card"><h3>Timetables</h3><a class="btn alt" href="{{route('teacher.timetables',$class)}}">View schedules</a></div></div></x-layouts.app>
